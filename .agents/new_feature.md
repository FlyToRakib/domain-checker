Here’s your instruction rewritten clearly without removing anything, but structured in a clean, industry-standard way:

---

**Improvement Implementation Plan (Industry Standard Approach)**

Let’s implement this improvement correctly following industry-standard practices.

### Updated Concept

The overlay will now be improved and updated with **two modes (tabs)**.

### Form Structure Update

The user input form will be divided into **two tabs**:

1. **Generate New Brand**
2. **Make Brand Unique**

---

### 1. Generate New Brand

* This tab is for users who want to generate **completely new brand/domain names**.
* The current flow and functionality will remain **exactly the same as now**.
* No changes to logic—only placed inside this tab.
* This tab will be **active by default**.

---

### 2. Make Brand Unique

* This tab is for users who already have a list of domains.
* If their preferred keyword/domain is already taken, they can use this tab to:

  * Add **prefixes and suffixes**
  * Make the domain more **logical, Unique, and targeted**

#### Changes in this tab:

* Move the following existing fields into this tab:

  * **Enable Prefix (Length)**
  * **Enable Suffix (Length)**

* Add a new input field:

  * **Domain Input Field**
  * Users can paste:

    * A single domain, or
    * Multiple domains (comma-separated or line-break separated)

* Add a button:

  * **Button Name:** *Regenerate Unique Brand*

* Add a new field:

  * **Flowing Rules**
  * Purpose:

    * Provide instructions for how to regenerate domains using prefixes/suffixes
    * Guide the AI to maintain logic and brand relevance
  * Behavior:

    * A default global instruction will be pre-filled
    * Users can **edit, add, or remove rules**

---

### Prompt System

* The **2nd tab (Make Brand Unique)** will have its own **separate prompt builder**, similar to the current one.

---

### Important Notes

* The **current form and system remain unchanged**, except for reorganizing fields into tabs.
* Ensure everything is implemented:

  * Without breaking any **UI**
  * Without breaking any **existing features**
  * Without breaking any **functionality**
* Follow proper **industry-standard implementation practices**

---
